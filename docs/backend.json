
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user of the ApnaHelper application, encompassing customers, service providers (workers), and administrators. Stores core profile information and role.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "firebaseUid": {
          "type": "string",
          "description": "Unique identifier provided by Firebase Authentication, linking this profile to an external authentication record."
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the user."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the user."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user, used for communication and authentication (derived from Firebase).",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "The role of the user within the application (Customer, Worker, or Admin).",
          "format": "enum",
          "items": {
            "type": "string"
          }
        },
        "phoneNumber": {
          "type": "string",
          "description": "The contact phone number of the user."
        },
        "apartment": {
          "type": "string",
          "description": "Apartment/building name and flat number."
        },
        "landmark": {
          "type": "string",
          "description": "Nearby landmark."
        },
        "city": {
          "type": "string",
          "description": "City name."
        },
        "state": {
          "type": "string",
          "description": "State name."
        },
        "address": {
          "type": "string",
          "description": "The full concatenated address of the user."
        },
        "latitude": {
          "type": "number",
          "description": "The latitude coordinate of the user's primary address, for location-based services."
        },
        "longitude": {
          "type": "number",
          "description": "The longitude coordinate of the user's primary address, for location-based services."
        },
        "profilePictureUrl": {
          "type": "string",
          "description": "URL to the user's profile picture.",
          "format": "uri"
        },
        "bio": {
          "type": "string",
          "description": "A short biographical description, primarily for Workers to describe their experience and services."
        },
        "status": {
          "type": "string",
          "description": "The current status of the user profile (e.g., active, banned, pending_verification).",
          "format": "enum",
          "items": {
            "type": "string"
          }
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the user profile was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "firebaseUid",
        "firstName",
        "lastName",
        "email",
        "role",
        "status",
        "createdAt"
      ]
    },
    "ServiceCategory": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ServiceCategory",
      "type": "object",
      "description": "Defines a category of service offered within the ApnaHelper platform (e.g., Plumbing, Electrical, HVAC).",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ServiceCategory entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the service category (e.g., 'Plumbing')."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the services included in this category."
        },
        "iconUrl": {
          "type": "string",
          "description": "URL to an icon representing the service category.",
          "format": "uri"
        }
      },
      "required": [
        "id",
        "name",
        "description"
      ]
    },
    "ServiceRequest": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ServiceRequest",
      "type": "object",
      "description": "Represents a service request posted by a Customer, detailing the job required, its status, and associated worker and ratings.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ServiceRequest entity."
        },
        "customerId": {
          "type": "string",
          "description": "Reference to the UserProfile ID of the customer who created the service request."
        },
        "workerId": {
          "type": "string",
          "description": "Reference to the UserProfile ID of the worker who was selected. Null if not yet selected."
        },
        "serviceType": {
          "type": "string",
          "description": "The category of service requested."
        },
        "title": {
          "type": "string",
          "description": "A concise title for the service request."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the service required."
        },
        "apartment": {
          "type": "string",
          "description": "Apartment/Building name with flat number."
        },
        "landmark": {
          "type": "string",
          "description": "Nearby landmark."
        },
        "areaCityPincode": {
          "type": "string",
          "description": "Area/City with pincode."
        },
        "state": {
          "type": "string",
          "description": "State name."
        },
        "status": {
          "type": "string",
          "description": "The current status of the service request (PENDING, ACCEPTED, IN_PROGRESS, COMPLETED)."
        },
        "workerRating": {
          "type": "number",
          "description": "Rating given by the customer to the worker (1-5)."
        },
        "workerFeedback": {
          "type": "string",
          "description": "Feedback given by the customer to the worker."
        },
        "customerRating": {
          "type": "number",
          "description": "Rating given by the worker to the customer (1-5)."
        },
        "customerFeedback": {
          "type": "string",
          "description": "Feedback given by the worker to the customer."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "customerId",
        "serviceType",
        "description",
        "apartment",
        "areaCityPincode",
        "state",
        "status",
        "createdAt"
      ]
    },
    "Quote": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Quote",
      "type": "object",
      "description": "A price offer sent by a worker for a specific service request.",
      "properties": {
        "id": {
          "type": "string"
        },
        "serviceRequestId": {
          "type": "string"
        },
        "workerId": {
          "type": "string"
        },
        "workerName": {
          "type": "string"
        },
        "price": {
          "type": "number"
        },
        "message": {
          "type": "string"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["id", "serviceRequestId", "workerId", "workerName", "price", "createdAt"]
    },
    "Report": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Report",
      "type": "object",
      "description": "A support ticket or incident report submitted by a user.",
      "properties": {
        "id": { "type": "string" },
        "reporterUserId": { "type": "string" },
        "subject": { "type": "string" },
        "description": { "type": "string" },
        "status": { "type": "string", "enum": ["OPEN", "IN_PROGRESS", "RESOLVED"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "reporterUserId", "subject", "description", "status", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          }
        }
      },
      {
        "path": "/service_requests/{serviceRequestId}",
        "definition": {
          "entityName": "ServiceRequest",
          "schema": {
            "$ref": "#/backend/entities/ServiceRequest"
          }
        }
      },
      {
        "path": "/service_requests/{serviceRequestId}/quotes/{quoteId}",
        "definition": {
          "entityName": "Quote",
          "schema": {
            "$ref": "#/backend/entities/Quote"
          }
        }
      },
      {
        "path": "/reports/{reportId}",
        "definition": {
          "entityName": "Report",
          "schema": {
            "$ref": "#/backend/entities/Report"
          }
        }
      }
    ]
  }
}
